.row-fluid
  .col-md-6.col-md-offset-3
    %h4
      %span= t "new_review_for"
      = link_to "#{@book.decorate.title_author}", @book

    = form_for [@book, @rating] do |f|
      = render 'form', f: f


